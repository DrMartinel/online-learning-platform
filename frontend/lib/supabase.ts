/**
 * DEPRECATED: Direct Supabase client access is no longer allowed.
 * All interactions must go through the proxy client in `@/lib/supabase-proxy`.
 * This prevents exposing internal Supabase URLs and bypasses CORS issues.
 */

export {};
